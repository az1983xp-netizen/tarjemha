# نشر ترجمها على Render — خطوات بسيطة

1. ارفع محتويات هذا المشروع إلى مستودع GitHub عام.
2. في Render اختر **New → Web Service**.
3. اختر **Public Git Repository** وأدخل رابط مستودع GitHub.
4. إذا ظهر إعداد Blueprint/Render YAML، استخدم `render.yaml`.
5. عند طلب `OPENAI_API_KEY` ضع مفتاح OpenAI في خانة السر فقط، ولا تضعه داخل الملفات.
6. بعد نجاح النشر افتح رابط `onrender.com` واختبر `/api/health`.

مهم:
- هذه النسخة مناسبة للمرحلة التجريبية الأولى.
- الدفع الحقيقي، OCR، التخزين الدائم، الحسابات، والبريد الإلكتروني ما زالت مراحل لاحقة قبل الإطلاق التجاري الكامل.
